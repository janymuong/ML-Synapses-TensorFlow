Convolutional Nueral Networks:

On top of DNNs and RNNs, let's add convolutions, and then piece it together using a real-world data series - one which measures sunspot activity over hundreds of years, and see if we can predict using it.